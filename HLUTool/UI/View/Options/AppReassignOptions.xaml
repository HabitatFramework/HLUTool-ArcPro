<UserControl
    x:Class="HLU.UI.View.AppReassignOptions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
    xmlns:local="clr-namespace:HLU.UI.ViewModel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:ViewModelWindowOptions}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
                <ResourceDictionary Source="../Dictionary/ErrorMessageConverterDictionary.xaml" />
                <ResourceDictionary Source="../Dictionary/ValidationTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="4">

            <Label
                HorizontalAlignment="Left"
                Content="Set application options for the Reassign Features process"
                FontSize="14"
                FontWeight="Medium" />

            <Expander
                Margin="6,10,0,0"
                Header="Reassign Rules"
                IsExpanded="True">

                <StackPanel Margin="12,4,4,4">

                    <TextBlock
                        Margin="0,0,0,6"
                        TextWrapping="Wrap"
                        Text="Define one or more rules used to assign features to a target HLU layer. Each rule requires a unique name and a SQL WHERE clause that will be applied to select matching features." />

                    <!--  Rules DataGrid  -->
                    <DataGrid
                        x:Name="DataGridReassignRules"
                        MinHeight="120"
                        MaxHeight="300"
                        AutoGenerateColumns="False"
                        CanUserAddRows="True"
                        CanUserDeleteRows="True"
                        CanUserReorderColumns="False"
                        GridLinesVisibility="Horizontal"
                        HeadersVisibility="Column"
                        ItemsSource="{Binding ReassignRules}"
                        SelectionMode="Single"
                        Style="{DynamicResource Esri_DataGridStyle}">

                        <DataGrid.Columns>

                            <DataGridTextColumn
                                Width="160"
                                Binding="{Binding RuleName, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                Header="Rule Name">
                                <DataGridTextColumn.EditingElementStyle>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationTemplate}" />
                                    </Style>
                                </DataGridTextColumn.EditingElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding WhereClause, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                Header="SQL WHERE Clause">
                                <DataGridTextColumn.EditingElementStyle>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationTemplate}" />
                                    </Style>
                                </DataGridTextColumn.EditingElementStyle>
                            </DataGridTextColumn>

                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock
                        Margin="0,4,0,0"
                        FontStyle="Italic"
                        Foreground="Gray"
                        Text="Click on a row to edit it. Press Delete to remove the selected row. Type in the last row to add a new rule." />

                </StackPanel>

            </Expander>

        </StackPanel>

        <!--  Help link  -->
        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Margin="5"
                Command="{Binding OpenHyperlinkCommand}"
                CommandParameter="{Binding Hyperlink_AppReassignHelp}"
                Content="Help"
                Style="{DynamicResource Esri_Button}" />
        </StackPanel>

    </Grid>
</UserControl>
