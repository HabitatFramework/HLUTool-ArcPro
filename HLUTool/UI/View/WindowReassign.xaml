<controls:ProWindow
    x:Class="HLU.UI.View.WindowReassign"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HLU.UI.ViewModel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding WindowTitle}"
    MinWidth="380"
    MinHeight="240"
    d:DataContext="{d:DesignInstance Type=local:ViewModelWindowReassign}"
    Icon="..\..\Images\HLUTool16.png"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="WidthAndHeight"
    Style="{DynamicResource ProWindowStyle}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Dictionary\ValidationTemplates.xaml" />
                <ResourceDictionary Source="Dictionary\ErrorMessageConverterDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>

    <DockPanel
        Width="Auto"
        Margin="4,4,4,4"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="4" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <GroupBox
                Name="GroupBoxReassign"
                Grid.Row="0"
                Header="Reassign Features">
                <Grid Name="GridReassign" Margin="2,8,2,2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Source Layer  -->
                    <Label
                        Name="LabelSourceLayer"
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="0,0,0,8"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                        Source Layer
                    </Label>
                    <TextBox
                        Name="TextBoxSourceLayer"
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="8,0,8,8"
                        IsEnabled="False"
                        Text="{Binding SourceLayerName}" />

                    <!--  Target Layer  -->
                    <Label
                        Name="LabelTargetLayer"
                        Grid.Row="1"
                        Grid.Column="0"
                        Margin="0,0,0,8"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                        Target Layer
                    </Label>
                    <ComboBox
                        Name="ComboBoxTargetLayer"
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="8,0,8,8"
                        IsEditable="False"
                        ItemsSource="{Binding TargetLayerNames}"
                        SelectedValue="{Binding TargetLayerName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                        Tag="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors).CurrentItem.ErrorContent, Converter={StaticResource errorMessageConverter}}"
                        ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors).CurrentItem.ErrorContent}"
                        Validation.ErrorTemplate="{StaticResource ErrorTemplate}" />

                    <!--  Rule  -->
                    <Label
                        Name="LabelRule"
                        Grid.Row="2"
                        Grid.Column="0"
                        Margin="0,0,0,8"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                        Rule
                    </Label>
                    <ComboBox
                        Name="ComboBoxRule"
                        Grid.Row="2"
                        Grid.Column="1"
                        Margin="8,0,8,8"
                        DisplayMemberPath="RuleName"
                        IsEditable="False"
                        ItemsSource="{Binding Rules}"
                        SelectedItem="{Binding SelectedRule, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                        Tag="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors).CurrentItem.ErrorContent, Converter={StaticResource errorMessageConverter}}"
                        ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors).CurrentItem.ErrorContent}"
                        Validation.ErrorTemplate="{StaticResource ErrorTemplate}" />

                    <!--  Feature Count  -->
                    <Label
                        Name="LabelFeatureCount"
                        Grid.Row="3"
                        Grid.Column="0"
                        Margin="0,0,0,2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                        Features
                    </Label>
                    <TextBox
                        Name="TextBoxFeatureCount"
                        Grid.Row="3"
                        Grid.Column="1"
                        Margin="8,0,8,2"
                        IsEnabled="False"
                        Text="{Binding FeatureCountText}" />
                </Grid>
            </GroupBox>

            <!--  OK / Cancel  -->
            <StackPanel
                Grid.Row="2"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Name="ButtonOk"
                    Width="65"
                    Margin="0,0,8,0"
                    Command="{Binding OkCommand}"
                    Content="OK"
                    IsDefault="True" />
                <Button
                    Name="ButtonCancel"
                    Width="65"
                    Command="{Binding CancelCommand}"
                    Content="Cancel"
                    IsCancel="True" />
            </StackPanel>
        </Grid>
    </DockPanel>
</controls:ProWindow>
