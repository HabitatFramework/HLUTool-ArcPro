<controls:ProWindow x:Class="HLU.UI.View.WindowEditPotentialHabitats"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
    xmlns:hlu="clr-namespace:HLU.UI.UserControls;assembly="
    xmlns:tkit="http://schemas.microsoft.com/wpf/2008/toolkit"
    Title="{Binding Path=WindowTitle}"
    Width="800"
    MinWidth="500"
    MinHeight="250"
    Icon="..\..\Icons\HLUIcon.png"
    ResizeMode="CanResize"
    ShowInTaskbar="False"
    SizeToContent="Height"
    Style="{DynamicResource ProWindowStyle}"
    WindowStartupLocation="CenterOwner">

    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Dictionary\CodeDescriptionConverterDictionary.xaml" />
                <ResourceDictionary Source="Dictionary\CodeNameDescriptionConverterDictionary.xaml" />
                <ResourceDictionary Source="Dictionary\ErrorMessageConverterDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ProWindow.Resources>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="4" />
            <RowDefinition Height="23" />
        </Grid.RowDefinitions>

        <GroupBox Header="Potential Priority Habitats"
            Name="GroupBoxFeatures"
            Grid.Row="0"
            Grid.Column="0">
            <DataGrid ItemsSource="{Binding IncidBapHabitatsUser}"
                x:Name="DataGridUserBapHabitats"
                Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Margin="4,2,6,2"
                Padding="2,0"
                AutoGenerateColumns="False"
                BorderBrush="Black"
                BorderThickness="1"
                CanUserAddRows="True"
                CanUserDeleteRows="True"
                CanUserReorderColumns="False"
                CanUserResizeColumns="True"
                CanUserSortColumns="True"
                HeadersVisibility="All"
                IsEnabled="{Binding BapHabitatsUserEnabled}"
                MinRowHeight="24"
                RowDetailsVisibilityMode="Visible"
                RowHeaderWidth="20"
                RowHeight="24"
                Style="{DynamicResource Esri_DataGrid}">
                <DataGrid.RowValidationRules>
                    <DataErrorValidationRule />
                </DataGrid.RowValidationRules>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="bap_id"
                        Binding="{Binding bap_id}"
                        IsReadOnly="True"
                        Visibility="Collapsed" />
                    <DataGridTextColumn Header="INCID"
                        Binding="{Binding incid}"
                        IsReadOnly="True"
                        Visibility="Collapsed" />
                    <DataGridComboBoxColumn Header="Habitat" ItemsSource="{Binding DataContext.BapHabitatCodes, IsAsync=true, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
                        Width="SizeToCells"
                        MinWidth="250"
                        CanUserResize="True"
                        DisplayMemberPath="description"
                        IsReadOnly="False"
                        SelectedValueBinding="{Binding bap_habitat,
                                                       UpdateSourceTrigger=PropertyChanged,
                                                       ValidatesOnDataErrors=True}"
                        SelectedValuePath="code" />
                    <!-- EditingElementStyle="{DynamicResource ComboBoxStyle}" -->
                    <DataGridComboBoxColumn Header="Determ. Quality" ItemsSource="{Binding DataContext.BapDeterminationQualityCodesUser, IsAsync=true, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
                        Width="SizeToCells"
                        MinWidth="200"
                        CanUserResize="True"
                        DisplayMemberPath="description"
                        SelectedValueBinding="{Binding quality_determination,
                                                       UpdateSourceTrigger=PropertyChanged,
                                                       ValidatesOnDataErrors=True}"
                        SelectedValuePath="code" />
                    <!-- EditingElementStyle="{DynamicResource ComboBoxStyle}" -->
                    <DataGridComboBoxColumn Header="Interp. Quality" ItemsSource="{Binding DataContext.BapInterpretationQualityCodes, IsAsync=true, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}"
                        Width="SizeToCells"
                        MinWidth="100"
                        CanUserResize="True"
                        DisplayMemberPath="description"
                        SelectedValueBinding="{Binding quality_interpretation,
                                                       UpdateSourceTrigger=PropertyChanged,
                                                       ValidatesOnDataErrors=True}"
                        SelectedValuePath="code" />
                    <!-- EditingElementStyle="{DynamicResource ComboBoxStyle}" -->
                    <DataGridTextColumn Header="Interp. Comments"
                        Width="SizeToCells"
                        MinWidth="200"
                        Binding="{Binding interpretation_comments,
                                          UpdateSourceTrigger=PropertyChanged,
                                          ValidatesOnDataErrors=True}"
                        CanUserResize="True" />
                    <!-- EditingElementStyle="{DynamicResource TextBoxStyle}" -->
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <Grid
            Name="GridButtons"
            Grid.Row="2"
            Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="82" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="82" />
                <ColumnDefinition Width="82" />
            </Grid.ColumnDefinitions>
            <Button
                Name="ButtonOK"
                Grid.Row="0"
                Grid.Column="2"
                Margin="0,0,2,0"
                Command="{Binding Path=OkCommand}"
                IsDefault="True"
                IsEnabled="{Binding Path=CanOk}">
                _OK
            </Button>
            <Button
                Name="ButtonCancel"
                Grid.Row="0"
                Grid.Column="3"
                Margin="2,0,0,0"
                Command="{Binding Path=CancelCommand}"
                IsCancel="True">
                _Cancel
            </Button>
        </Grid>
    </Grid>
</controls:ProWindow>